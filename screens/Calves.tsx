import React from 'react';
import { View, Text } from 'react-native';

export const Calves = () => (
	<View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
		<Text>Calves Screen</Text>
	</View>
);
